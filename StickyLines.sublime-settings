{
	// Enables auto follow thread
	// Disabling this settings basically disables sticky lines features
	// Other features like on demand context are still available
	"sticky_lines_auto_follow": true,

	// Default behavior of sticky lines on a view
	// This can be overridden on a per view basis
	"sticky_lines_enabled_globally": true,
}